spring:
  application:
    name: libraryap
  datasource:
    url: jdbc:postgresql://localhost:5432/library
    username: root
    password: root
    driver-class-name: org.postgresql.Driver
  jpa:
    show-sql: true
    hibernate:
        ddl-auto: update
    properties:
      hibernate:
        format_sql: true

